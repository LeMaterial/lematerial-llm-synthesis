# Defaults
defaults:
  - _self_
  - data_loader: null
  - synthesis_extraction: default
  - material_extraction: default # changed from null
  - paragraph_extraction: null
  - judge: null
  - result_save: default # changed from null

# Dir handling
hydra:
  job:
    chdir: true
  run:
    dir: results/single_run/${now:%Y-%m-%d}/${now:%H-%M-%S}
  sweep:
    dir: results/multi_run/${now:%Y-%m-%d}/${now:%H-%M-%S}
